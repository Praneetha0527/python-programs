class Solution(object):
    def maxFreqSum(self, s):
        c=0
        v=0
        n=set(s)
        for i in n:
            if i in "aeiou":
                v=max(v, s.count(i))
            else:
                c=max(c, s.count(i))
        return c+v  
